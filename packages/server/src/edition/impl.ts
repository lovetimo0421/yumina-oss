/**
 * Open-source edition. Only the local implementation exists in this tree; the
 * hosted one (yumina.io's hub, billing, community, admin) lives in the private
 * monorepo this repository is exported from. See CONTRIBUTING.md.
 */
export { localEdition as edition } from "./local.js";
