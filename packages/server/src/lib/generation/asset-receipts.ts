/**
 * Open-source edition stub. Image generation (and its credit receipts) is a
 * hosted feature; deleting an asset locally has nothing to detach.
 */
export async function detachGenerationAsset(_database: unknown, _userId: string, _assetId: string): Promise<void> {}
